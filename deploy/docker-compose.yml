version: '3'
services:

    elk:
        image: sebp/elk
        container_name: elk
        # environment: 
        #     - MAX_MAP_COUNT=262144
        ports:
            # kibana
            - "5601:5601"
            # elasticsearch
            - "9200:9200"
            # logstash
            - "5044:5044"    


networks:
    default:
        external:
            name: logs_network    